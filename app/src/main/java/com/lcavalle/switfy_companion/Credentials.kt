package com.lcavalle.switfy_companion

/**
 * Contains the credentials for the 42 API.
 * This is just the sample, the credentials won't be uploaded to Git.
 * They need to be set manually after cloning the repository.
 * You can find them in 42 intra profile, API section, after the application has been created.
 * Keep in mind that the Secret has a lifetime of 1 month approx.
 */

class Credentials {
    val UID: String = ""
    val Secret: String = ""
}